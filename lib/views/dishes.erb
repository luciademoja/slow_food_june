<% if @dishes.any? %>
  <div class="row">
    <div class="row column">
      <h2 style="text-align:center;">Make a selection from our mouth watering dishes</h2>
    <div class="row small-up-1 medium-up-2 large-up-3">
      <% @dishes.each do |dish| %>
        <div class="column">
          <div class="callout">
            <p><img src="http://placehold.it/400x370&text=Pegasi B" alt="image of a planet called Pegasi B"></p>
            <p class="lead"><%= dish.name %></p>
            <p class="subheader"><%= dish.description %></p>
            <p class="subheader"><%= dish.price %></p>
            <form action="/add_to_order" method="post" id="<%= dish.id %>">
              <input type="hidden" value="<%= dish.id %>" name="item">
              <input type="submit" value="Add to order">
            </form>
          </div>
        </div>
      <% end %>
    </div>
  </div>

<% else %>
    <h2 style="text-align:center;">There are no dishes to display</h2>
<% end %>
